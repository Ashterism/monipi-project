[project]
name = "monipi"
version = "1.0.0"
requires-python = ">=3.10"

dependencies = [
    "flask",
    "pms5003",
    "sensirion-i2c-scd30"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]